# wg-fleet Environment Configuration Example
# Copy this file to .env and customize as needed

# Database path
WG_FLEET_DB_PATH=/var/lib/wg-fleet/clients.db

# Configuration file
WG_FLEET_CONFIG=/etc/wg-fleet.yaml

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Server host and port (optional, defaults are shown)
# WG_FLEET_HOST=0.0.0.0
# WG_FLEET_PORT=8000

# Hosts file output path (optional, default is shown)
# WG_FLEET_HOSTS_FILE=/run/fleet_hosts

# Pruning interval in seconds (optional, default is 300 = 5 minutes)
# WG_FLEET_PRUNE_INTERVAL=300
